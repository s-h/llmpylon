# 复制为 .env 后按需修改（本仓库示例不含敏感信息）
# Copy to .env and adjust as needed.

# HTTP 端口（默认 3000）
# PORT=3000

# SQLite 数据库文件路径（Docker 镜像内默认 /data/database.sqlite）
# DB_PATH=./database.sqlite

# 运行环境：生产环境可设为 production
# NODE_ENV=production
